<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Sozialize" %></title>
      <metaname="description" content="<%=content_for? (:description) ? 
      yield(:description) : "Sozialize" %>">
                  
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag '//ajax.googleapis.com/ajax/libs/
                                jqueryui/1.8/jquery-ui.min.js' %>
    <%= stylesheet_link_tag "//ajax.googleapis.com/ajax/libs/jqueryui/
                             1.8/themes/smoothness/jquery-ui.css" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    
    <header>
      <%= render 'layouts/header' %>
    </header>
    
    <div class="container">	
	    <% flash.each do |key, value| %>
        <% value.each do |val|  %>
	        <%= content_tag(:div, val, class: "alert alert-#{key}") %>
	      <% end %>
      <% end %>
        
      <%= yield %>
      
      <footer>
        <%= render 'layouts/footer' %>
      </footer>
        
      <%= debug(params) if Rails.env.development? %>
      <%= debug(session) if Rails.env.development? %>
    </div>
  </body>
</html>